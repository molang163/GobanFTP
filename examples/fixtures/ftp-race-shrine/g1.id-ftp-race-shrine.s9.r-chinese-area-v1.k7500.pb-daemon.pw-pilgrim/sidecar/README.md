# Race Shrine Sidecar

These notes are marginalia. They may explain why the daemon acknowledged the
left race child, but they do not resolve the fork.

Default replay reads only the game descriptor basename and direct `events/`
basenames. Ack-assisted recovery is explicit policy, not sidecar truth.
