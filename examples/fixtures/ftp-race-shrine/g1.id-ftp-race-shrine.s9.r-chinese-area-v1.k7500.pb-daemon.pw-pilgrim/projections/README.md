# Race Shrine Projections

These files are conservative projections, not truth.

They are derived from:

```text
game descriptor basename
direct events/ child basenames
```

The ack event is visible in the listing, but default projection remains
conservative: `oracle/verdict.txt` reports the fork and `sgf/main.sgf` stops at
the fork parent. Explicit ack-assisted replay is tested separately.
